diff --git a/build.sh b/build.sh
index 96e275fe..5bb6cabc 100755
--- a/build.sh
+++ b/build.sh
@@ -95,8 +95,15 @@ else
     ASCEND_INCLUDE_DIR=${_ASCEND_INSTALL_PATH}/aarch64-linux/include
 fi
 
+if [ -n "$SHMEM_HOME_PATH" ]; then
+    _SHMEM_HOME_PATH=$SHMEM_HOME_PATH
+else
+    _SHMEM_HOME_PATH=/usr/local/Ascend/shmem/latest
+fi
+
 export ASCEND_TOOLKIT_HOME=${_ASCEND_INSTALL_PATH}
 export ASCEND_HOME_PATH=${_ASCEND_INSTALL_PATH}
+export SHMEM_HOME_PATH=${_SHMEM_HOME_PATH}
 echo "ascend path: ${ASCEND_HOME_PATH}"
 source $(dirname ${ASCEND_HOME_PATH})/set_env.sh
 
@@ -122,7 +129,7 @@ function build_kernels()
     rm -rf $BUILD_DIR
     mkdir -p $BUILD_DIR
 
-    cmake $COMPILE_OPTIONS -DCMAKE_INSTALL_PREFIX="$OUTPUT_DIR" -DASCEND_HOME_PATH=$ASCEND_HOME_PATH -DASCEND_INCLUDE_DIR=$ASCEND_INCLUDE_DIR -DSOC_VERSION=$SOC_VERSION -DBUILD_DEEPEP_MODULE=$BUILD_DEEPEP_MODULE -DBUILD_KERNELS_MODULE=$BUILD_KERNELS_MODULE -B "$BUILD_DIR" -S .
+    cmake $COMPILE_OPTIONS -DCMAKE_INSTALL_PREFIX="$OUTPUT_DIR" -DASCEND_HOME_PATH=$ASCEND_HOME_PATH -DASCEND_INCLUDE_DIR=$ASCEND_INCLUDE_DIR -DSHMEM_HOME_PATH=$SHMEM_HOME_PATH -DSOC_VERSION=$SOC_VERSION -DBUILD_DEEPEP_MODULE=$BUILD_DEEPEP_MODULE -DBUILD_KERNELS_MODULE=$BUILD_KERNELS_MODULE -B "$BUILD_DIR" -S .
     cmake --build "$BUILD_DIR" -j8 && cmake --build "$BUILD_DIR" --target install
     cd -
 }
diff --git a/csrc/deepep/CMakeLists.txt b/csrc/deepep/CMakeLists.txt
index b5147573..c046e50f 100644
--- a/csrc/deepep/CMakeLists.txt
+++ b/csrc/deepep/CMakeLists.txt
@@ -12,7 +12,7 @@ endif()
 set(PROJECT_BUILD_PATH ${PROJECT_BINARY_DIR})
 set(TARGET_INSTALL_DIR ${CMAKE_INSTALL_PREFIX})
 set(ASCEND_HOME_PATH ${ASCEND_HOME_PATH})
-
+set(SHMEM_HOME_PATH ${SHMEM_HOME_PATH})
 find_program(PYTHON_EXECUTABLE NAMES python3 python)
 message(STATUS "PYTHON_EXECUTABLE:${PYTHON_EXECUTABLE}")
 
@@ -36,11 +36,13 @@ target_include_directories( deep_ep_cpp PRIVATE
     ${TORCH_NPU_DIR}/include/third_party/acl/inc
     ${ASCEND_HOME_PATH}/include
     ${ASCEND_HOME_PATH}/${DEEPEP_ARCH}-linux/include/experiment/platform
+    ${SHMEM_HOME_PATH}/shmem/include
 )
 target_link_directories(deep_ep_cpp PRIVATE
         ${TORCH_DIR}/lib
         ${TORCH_NPU_DIR}/lib
         ${ASCEND_HOME_PATH}/lib64
+        ${SHMEM_HOME_PATH}/shmem/lib
 )
 target_link_libraries(deep_ep_cpp PRIVATE
     torch_python
@@ -48,6 +50,7 @@ target_link_libraries(deep_ep_cpp PRIVATE
     hccl
     torch_npu
     opapi
+    shmem
 )
 
 message(STATUS "TARGET_INSTALL_DIR = ${TARGET_INSTALL_DIR}")
diff --git a/csrc/deepep/deep_ep.cpp b/csrc/deepep/deep_ep.cpp
index 2f72c85a..ec438734 100644
--- a/csrc/deepep/deep_ep.cpp
+++ b/csrc/deepep/deep_ep.cpp
@@ -6,6 +6,7 @@
 #include "exception.hpp"
 #include "deep_ep.hpp"
 #include "pytorch_npu_helper.hpp"
+#include "shmem.hpp"
 
 namespace deep_ep {
 constexpr int PADDING_SIZE = 1;
@@ -54,9 +55,22 @@ Buffer::Buffer(int64_t rank, int64_t num_ranks, int64_t num_nvl_bytes, int64_t n
         rdma_rank = rank / A2_MAX_HCCS_PEERS;
         nvl_rank = rank % A2_MAX_HCCS_PEERS;
     }
+
+    size_t local_mem_size = 2 * 1024 * 1024 * 1024UL;
+    size_t meta_data_size = 100 * 1024 * 1024UL;
+    EP_HOST_ASSERT(rank == internode::init(rank, num_ranks, local_mem_size, "tcp://127.0.0.1:11222"));
+    shmem_ptr = internode::alloc(meta_data_size, NUM_BUFFER_ALIGNMENT_BYTES);
+    std::cout << "rank: " << rank << ", num_ranks: " << num_ranks << ", shmem_ptr: " << shmem_ptr << std::endl;
 }
 
-Buffer::~Buffer() noexcept(false) {}
+Buffer::~Buffer() noexcept(false)
+{
+    std::cout << "rank " << rank << " ~Buffer" << std::endl;
+    internode::free(shmem_ptr);
+    std::cout << "rank " << rank << " free done!!!" << std::endl;
+    internode::finalize();
+    std::cout << "rank " << rank << " finalize done!!!" << std::endl;
+}
 
 bool Buffer::is_available() const
 {
diff --git a/csrc/deepep/deep_ep.hpp b/csrc/deepep/deep_ep.hpp
index 35810b11..ae90ab9c 100644
--- a/csrc/deepep/deep_ep.hpp
+++ b/csrc/deepep/deep_ep.hpp
@@ -37,6 +37,7 @@ struct Buffer {
 
 private:
     std::string moe_all_to_all_group_name;
+    void *shmem_ptr = nullptr;
 
     int device_id;
 
diff --git a/csrc/deepep/ops/cmake/util/ascendc_impl_build.py b/csrc/deepep/ops/cmake/util/ascendc_impl_build.py
index 7486ad13..fd9c2ffa 100755
--- a/csrc/deepep/ops/cmake/util/ascendc_impl_build.py
+++ b/csrc/deepep/ops/cmake/util/ascendc_impl_build.py
@@ -163,6 +163,19 @@ def {}({}, kernel_name="{}", impl_mode=""):
     options.append("-I" + os.path.join(tikcpp_path, "tikcfw", "impl"))
     options.append("-I" + os.path.join(tikcpp_path, "tikcfw", "interface"))
     options.append("-I" + os.path.join(PYF_PATH, "..", "ascendc", "common"))
+
+    SHMEM_HOME_PATH = os.environ.get('SHMEM_HOME_PATH')
+    if SHMEM_HOME_PATH is None:
+        SHMEM_HOME_PATH = os.path.realpath("/usr/local/Ascend/shmem/latest")
+
+    options.append("-I" + SHMEM_HOME_PATH + "/shmem/include")
+    options.append("-I" + SHMEM_HOME_PATH + "shmem/include/device")
+    #options.append("-I" + SHMEM_HOME_PATH +"/shmem/include/host")
+    options.append("-I" + SHMEM_HOME_PATH + "/shmem/include/host_device")
+    options.append("-I" + SHMEM_HOME_PATH + "/shmem/include/internal")
+    options.append("-I" + SHMEM_HOME_PATH + "/memfabric_hybrid/include/smem/device")
+    #options.append("-I" + SHMEM_HOME_PATH + "/memfabric_hybrid/include/smem/host")
+
     if impl_mode == "high_performance":
         options.append("-DHIGH_PERFORMANCE=1")
     elif impl_mode == "high_precision":
diff --git a/csrc/deepep/ops/op_host/CMakeLists.txt b/csrc/deepep/ops/op_host/CMakeLists.txt
index 42f799c1..d138ef3d 100644
--- a/csrc/deepep/ops/op_host/CMakeLists.txt
+++ b/csrc/deepep/ops/op_host/CMakeLists.txt
@@ -6,6 +6,7 @@ aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR} ops_srcs)
 # =====================================================
 set(CANN82_PATH "${ASCEND_CANN_PACKAGE_PATH}/include/experiment/platform/platform/platform_infos_def.h")
 set(CANN83_PATH "${ASCEND_CANN_PACKAGE_PATH}/include/platform/platform_infos_def.h")
+set (SHMEM_HOME_PATH /usr/local/Ascend/shmem/latest)
 
 message(STATUS "ASCEND_CANN_PACKAGE_PATH = ${ASCEND_CANN_PACKAGE_PATH}")
 message(STATUS "Checking for CANN include paths:")
@@ -113,6 +114,7 @@ if(ENABLE_CROSS_COMPILE)
     target_link_directories(cust_opapi PRIVATE
                             ${CMAKE_COMPILE_COMPILER_LIBRARY}
                             ${CMAKE_COMPILE_RUNTIME_LIBRARY}
+                            shmem
     )
 endif()
 if(NOT ASCEND_PACK_SHARED_LIBRARY)
@@ -168,7 +170,7 @@ else()
     add_dependencies(cust_opapi ascend_opregistry)
 endif()
 
-target_include_directories(cust_opapi PRIVATE $ENV{ASCEND_HOME_PATH}/${CANN_HOST_ARCH}-linux/include/experiment/platform/)
+target_include_directories(cust_opapi PRIVATE $ENV{ASCEND_HOME_PATH}/${CANN_HOST_ARCH}-linux/include/experiment/platform/ ${SHMEM_HOME_PATH}/shmem/include)
 include_directories($ENV{ASCEND_HOME_PATH}/../opp/vendors/CAM/op_impl/ai_core/tbe/CAM_impl/dynamic/)
 
 add_custom_target(optiling_compat ALL
diff --git a/csrc/deepep/shmem.hpp b/csrc/deepep/shmem.hpp
new file mode 100644
index 00000000..d41ab047
--- /dev/null
+++ b/csrc/deepep/shmem.hpp
@@ -0,0 +1,51 @@
+#pragma once
+
+#include <iostream>
+
+#include "shmem_api.h"
+#include "config.hpp"
+#include "exception.hpp"
+
+#define NUM_BUFFER_ALIGNMENT_BYTES 128
+
+namespace deep_ep {
+namespace internode {
+
+int init(int rank, int num_ranks, uint64_t localMemSize, const char *server_ipport)
+{
+    std::cout << "rank: " << rank << " num_ranks: " << num_ranks << " localMemSize: " << localMemSize << " init start~"
+              << std::endl;
+    int32_t status = 0;
+    shmem_set_conf_store_tls(false, nullptr, 0);
+    shmem_init_attr_t *attributes;
+    status = shmem_set_attr(rank, num_ranks, localMemSize, server_ipport, &attributes);
+    EP_HOST_ASSERT(status == SHMEM_SUCCESS);
+    status = shmem_init_attr(attributes);
+    EP_HOST_ASSERT(status == SHMEM_SUCCESS);
+    EP_HOST_ASSERT(shmem_init_status() == SHMEM_STATUS_IS_INITIALIZED);
+    std::cout << "rank: " << rank << " num_ranks: " << num_ranks << " init done!" << std::endl;
+
+    return shmem_my_pe();
+}
+
+void *alloc(size_t size, size_t alignment)
+{
+    return shmem_align(alignment, size);
+}
+
+void free(void *ptr)
+{
+    shmem_free(ptr);
+}
+
+// void barrier() {
+//     shmem_barrier_all();
+// }
+
+void finalize()
+{
+    EP_HOST_ASSERT(shmem_finalize() == SHMEM_SUCCESS);
+}
+}  // namespace internode
+
+}  // namespace deep_ep
