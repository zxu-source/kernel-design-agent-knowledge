diff --git a/.github/workflows/build_and_release.yml b/.github/workflows/build_and_release.yml
index eebbd43a..1303ca2b 100644
--- a/.github/workflows/build_and_release.yml
+++ b/.github/workflows/build_and_release.yml
@@ -40,6 +40,8 @@ jobs:
     steps:
       - name: Checkout repository
         uses: actions/checkout@v6
+        with:
+          submodules: recursive
 
       - name: Mark repository safe
         run: |
diff --git a/.github/workflows/daily-build-test.yml b/.github/workflows/daily-build-test.yml
index a608acae..c372448e 100644
--- a/.github/workflows/daily-build-test.yml
+++ b/.github/workflows/daily-build-test.yml
@@ -41,6 +41,7 @@ jobs:
         uses: actions/checkout@v4
         with:
           clean: true
+          submodules: recursive
 
       - name: Install dependencies
         run: |
@@ -93,6 +94,7 @@ jobs:
       - uses: actions/checkout@v4
         with:
           clean: true
+          submodules: recursive
 
       - name: Install dependencies
         run: |
diff --git a/.github/workflows/internode.yml b/.github/workflows/internode.yml
index 2d373725..f86b60cb 100644
--- a/.github/workflows/internode.yml
+++ b/.github/workflows/internode.yml
@@ -50,6 +50,8 @@ jobs:
     steps:
       - name: Checkout code
         uses: actions/checkout@v4
+        with:
+          submodules: recursive
 
       - name: Install system dependencies
         run: |
diff --git a/.github/workflows/pr-test-npu.yml b/.github/workflows/pr-test-npu.yml
index bc23de32..248b3d51 100644
--- a/.github/workflows/pr-test-npu.yml
+++ b/.github/workflows/pr-test-npu.yml
@@ -77,12 +77,13 @@ jobs:
         run: |
           sudo rm -rf --one-file-system "$GITHUB_WORKSPACE"/* "$GITHUB_WORKSPACE"/.* 2>/dev/null || true
 
-      - name: Checkout code
-        uses: actions/checkout@v4
-        with:
-          clean: true
-
-      - name: Install dependencies
+      - name: Checkout code
+        uses: actions/checkout@v4
+        with:
+          clean: true
+          submodules: recursive
+
+      - name: Install dependencies
         run: |
           # speed up by using infra cache services
           CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
@@ -427,12 +428,13 @@ jobs:
         run: |
           sudo rm -rf --one-file-system "$GITHUB_WORKSPACE"/* "$GITHUB_WORKSPACE"/.* 2>/dev/null || true
 
-      - name: Checkout code
-        uses: actions/checkout@v4
-        with:
-          clean: true
-
-      - name: Install dependencies
+      - name: Checkout code
+        uses: actions/checkout@v4
+        with:
+          clean: true
+          submodules: recursive
+
+      - name: Install dependencies
         run: |
           # speed up by using infra cache services
           CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
@@ -783,12 +785,13 @@ jobs:
         run: |
           sudo rm -rf --one-file-system "$GITHUB_WORKSPACE"/* "$GITHUB_WORKSPACE"/.* 2>/dev/null || true
 
-      - name: Checkout code
-        uses: actions/checkout@v4
-        with:
-          clean: true
-
-      - name: Install dependencies
+      - name: Checkout code
+        uses: actions/checkout@v4
+        with:
+          clean: true
+          submodules: recursive
+
+      - name: Install dependencies
         run: |
           # speed up by using infra cache services
           CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
diff --git a/.github/workflows/push_build_cache.yml b/.github/workflows/push_build_cache.yml
index 8a953acb..c1cb380e 100644
--- a/.github/workflows/push_build_cache.yml
+++ b/.github/workflows/push_build_cache.yml
@@ -35,6 +35,8 @@ jobs:
 
       - name: Checkout code
         uses: actions/checkout@v4
+        with:
+          submodules: recursive
 
       - name: Get build hash
         id: get_build_hash
@@ -86,6 +88,8 @@ jobs:
 
       - name: Checkout code
         uses: actions/checkout@v4
+        with:
+          submodules: recursive
 
       - name: Get build hash
         id: get_build_hash
diff --git a/README.md b/README.md
index 142b8be6..7ddb5c1f 100644
--- a/README.md
+++ b/README.md
@@ -87,6 +87,11 @@ If you need to access Ascend NPU computing resources for development or testing,
 
 ## Quick Start
 
+Before building from source, initialize the submodule(s):
+```bash
+git submodule update --init --recursive
+```
+
 DeepEP-Ascend: Ascend Implementation of DeepEP. [README](https://github.com/sgl-project/sgl-kernel-npu/blob/main/python/deep_ep/README.md)
 
 SGLang-Kernel-NPU: Other SGLang Kernels for Ascend NPU. [README](https://github.com/sgl-project/sgl-kernel-npu/blob/main/python/sgl_kernel_npu/README.md)
diff --git a/build.sh b/build.sh
index 34fc9ca9..afc99cdb 100755
--- a/build.sh
+++ b/build.sh
@@ -137,22 +137,12 @@ echo "outpath: ${OUTPUT_DIR}"
 
 COMPILE_OPTIONS=""
 
-function update_submodules()
-{
-    PTO_ISA_DIR="third-party/pto-isa"
-    if [[ -d "${PTO_ISA_DIR}/include/pto" ]]; then return 0; fi
-
-    echo "Updating submodule: ${PTO_ISA_DIR}"
-    git submodule update --init --recursive "${PTO_ISA_DIR}"
-}
 
 function build_kernels()
 {
     if [[ "$ONLY_BUILD_DEEPEP_KERNELs_MODULE" == "ON" ]]; then return 0; fi
     if [[ "$ONLY_BUILD_MEMORY_SAVER_MODULE" == "ON" ]]; then return 0; fi
 
-    update_submodules
-
     CMAKE_DIR=""
     BUILD_DIR="build"
 
diff --git a/scripts/prepare_deepep_in_container.sh b/scripts/prepare_deepep_in_container.sh
index 22b7b11f..ffaaca50 100644
--- a/scripts/prepare_deepep_in_container.sh
+++ b/scripts/prepare_deepep_in_container.sh
@@ -17,11 +17,6 @@ done
 
 shift $((OPTIND -1))
 
-# build.sh now uses git to init submodules
-if [[ -n "${GITHUB_WORKSPACE:-}" ]]; then
-    git config --global --add safe.directory "${GITHUB_WORKSPACE}"
-fi
-
 cd ${GITHUB_WORKSPACE}
 if [ -n "$BUILD_ARGS" ]; then
     bash build.sh -a "$BUILD_ARGS"
diff --git a/tests/python/deepep/run_ascend_testcase.sh b/tests/python/deepep/run_ascend_testcase.sh
index dbc769b6..8523cbc6 100644
--- a/tests/python/deepep/run_ascend_testcase.sh
+++ b/tests/python/deepep/run_ascend_testcase.sh
@@ -26,7 +26,7 @@ pip config set global.extra-index-url "https://pypi.tuna.tsinghua.edu.cn/simple"
 pip config set global.trusted-host "${CACHING_URL} pypi.tuna.tsinghua.edu.cn"
 
 pip3 install kubernetes
-pip3 install xgrammar==0.1.25
+pip3 install --no-deps xgrammar==0.1.25
 
 unset https_proxy
 unset http_proxy
