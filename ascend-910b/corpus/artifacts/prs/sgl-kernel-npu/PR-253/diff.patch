diff --git a/.github/workflows/build_and_release.yml b/.github/workflows/build_and_release.yml
index 625f1f2e..715321f7 100644
--- a/.github/workflows/build_and_release.yml
+++ b/.github/workflows/build_and_release.yml
@@ -10,6 +10,7 @@ on:
       - 'main'
     paths:
       - '.github/workflows/build_and_release.yml'
+      - 'config.ini'
 
 jobs:
 
@@ -18,7 +19,7 @@ jobs:
     permissions:
       contents: write
     container:
-      image: quay.io/ascend/cann:8.3.rc1-a3-ubuntu22.04-py3.11
+      image: quay.io/ascend/cann:8.3.rc2-a3-ubuntu22.04-py3.11
     defaults:
       run:
         shell: bash
diff --git a/build.sh b/build.sh
index 96e275fe..8aefe418 100755
--- a/build.sh
+++ b/build.sh
@@ -190,6 +190,7 @@ function make_sgl_kernel_npu_package()
     cd python/sgl_kernel_npu || exit
 
     rm -rf "$CURRENT_DIR"/python/sgl_kernel_npu/dist
+    cp -v "${CURRENT_DIR}/config.ini" "${CURRENT_DIR}/python/sgl_kernel_npu/sgl_kernel_npu/"
     python3 setup.py clean --all
     python3 setup.py bdist_wheel
     mv -v "$CURRENT_DIR"/python/sgl_kernel_npu/dist/sgl_kernel_npu*.whl ${OUTPUT_DIR}/
diff --git a/config.ini b/config.ini
new file mode 100644
index 00000000..0cfa9154
--- /dev/null
+++ b/config.ini
@@ -0,0 +1,2 @@
+[global]
+version = 2025.12.14
diff --git a/python/sgl_kernel_npu/setup.py b/python/sgl_kernel_npu/setup.py
index 94c5c244..1cedc849 100755
--- a/python/sgl_kernel_npu/setup.py
+++ b/python/sgl_kernel_npu/setup.py
@@ -4,19 +4,17 @@
 """python api for sgl_kernel_npu."""
 
 import os
+from configparser import ConfigParser
+from pathlib import Path
 
 import setuptools
-import torch
-import torch_npu
-from setuptools import find_namespace_packages, find_packages, setup
+from setuptools import find_namespace_packages
 from setuptools.command.build_ext import build_ext
 from setuptools.dist import Distribution
 from torch_npu.utils.cpp_extension import NpuExtension
 
 os.environ["SOURCE_DATE_EPOCH"] = "0"
 
-current_version = os.getenv("VERSION", "0.1.0")
-
 
 class BinaryDistribution(Distribution):
     """Distribution which always forces a binary package with platform name"""
@@ -34,9 +32,15 @@ class Build(build_ext, object):
         super(Build, self).run()
 
 
+WORKING_DIR = Path(__file__).resolve().parent
+config = ConfigParser()
+config.read(WORKING_DIR / "sgl_kernel_npu" / "config.ini")
+_version = config.get("global", "version")
+
+
 setuptools.setup(
     name="sgl_kernel_npu",
-    version=current_version,
+    version=_version,
     description="python api for sgl_kernel_npu",
     packages=find_namespace_packages(exclude=("tests*",)),
     ext_modules=[NpuExtension("sgl_kernel_npu._C", sources=[])],
