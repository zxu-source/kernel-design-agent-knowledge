diff --git a/.github/workflows/pr-test-npu.yml b/.github/workflows/pr-test-npu.yml
index 7ca34d60..dde0cc99 100644
--- a/.github/workflows/pr-test-npu.yml
+++ b/.github/workflows/pr-test-npu.yml
@@ -93,7 +93,7 @@ jobs:
       - name: Run test
         timeout-minutes: 10
         env:
-          HCCL_BUFFSIZE: 2048
+          HCCL_BUFFSIZE: 3000
         run: |
           python3 $GITHUB_WORKSPACE/tests/python/deepep/test_intranode.py
 
diff --git a/csrc/deepep/deep_ep.cpp b/csrc/deepep/deep_ep.cpp
index 529cdbcc..a6895066 100644
--- a/csrc/deepep/deep_ep.cpp
+++ b/csrc/deepep/deep_ep.cpp
@@ -1,4 +1,5 @@
 #include <memory>
+#include <cmath>
 #include <pybind11/functional.h>
 
 #include "hccl/hccl.h"
@@ -10,6 +11,7 @@
 namespace deep_ep {
 constexpr int PADDING_SIZE = 3;
 constexpr size_t HCOMM_NAME_LEN = 128;
+constexpr double SCALE_SIZE = 1.5;
 
 Buffer::Buffer(int64_t rank, int64_t num_ranks, int64_t num_nvl_bytes, int64_t num_rdma_bytes, bool low_latency_mode,
                std::string moe_all_to_all_group_name)
@@ -310,7 +312,8 @@ Buffer::intranode_dispatch(const at::Tensor& x, const std::optional<at::Tensor>&
     int64_t tp_size = 1;
     int64_t tp_rank = 0;
     int64_t quant_mode = 0;
-    int64_t global_bs = std::max(num_max_dispatch_tokens_per_rank * num_ranks, static_cast<int64_t>(num_worst_tokens));
+    int64_t global_bs = static_cast<int64_t>(std::ceil(
+        std::max(num_max_dispatch_tokens_per_rank * num_ranks, static_cast<int64_t>(num_worst_tokens)) * SCALE_SIZE));
 
     auto send_token_idx = send_token_idx_cpu.to(x.device());
     auto recv_offset = recv_offset_cpu.to(x.device());
diff --git a/scripts/prepare_deepep_in_container.sh b/scripts/prepare_deepep_in_container.sh
index 7b33b661..9baf6297 100644
--- a/scripts/prepare_deepep_in_container.sh
+++ b/scripts/prepare_deepep_in_container.sh
@@ -3,4 +3,3 @@ bash build.sh
 pip install ${GITHUB_WORKSPACE}/output/deep_ep*.whl --no-cache-dir
 cd "$(pip show deep-ep | awk '/^Location:/ {print $2}')"
 ln -s deep_ep/deep_ep_cpp*.so || true
-export HCCL_BUFFSIZE=2048
