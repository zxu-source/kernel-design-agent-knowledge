diff --git a/csrc/deepep/ops/op_host/cam_moe_combine_normal_tiling.cc b/csrc/deepep/ops/op_host/cam_moe_combine_normal_tiling.cc
index 86eb344a..35cc1041 100644
--- a/csrc/deepep/ops/op_host/cam_moe_combine_normal_tiling.cc
+++ b/csrc/deepep/ops/op_host/cam_moe_combine_normal_tiling.cc
@@ -556,7 +556,7 @@ static ge::graphStatus CamMoeCombineNormalA3TilingFuncImpl(gert::TilingContext *
     uint32_t maxRound = tilingData->camMoeCombineNormalInfo.maxRound;
     // combine数据区 token首地址对齐512
     uint64_t tokenNeedSizeCombine = ((h * MAX_OUT_DTYPE_SIZE + WIN_ADDR_ALIGN - 1UL) / WIN_ADDR_ALIGN) * WIN_ADDR_ALIGN;
-    tokenNeedSizeCombine = maxRound > 1 ? maxRound * 2 : maxRound;
+    tokenNeedSizeCombine = maxRound > 1 ? tokenNeedSizeCombine * 2 : tokenNeedSizeCombine;
     uint64_t actualSize = (realBs * k * tokenNeedSizeCombine + COMBINE_STATE_WIN_OFFSET + NOTIFY_DISPATCH_WIN_OFFSET) *
                           DOUBLE_DATA_BUFFER;
     OP_TILING_CHECK(
diff --git a/csrc/deepep/ops/utils/op_kernel/operator/catlass/act/gemv_coord.hpp b/csrc/deepep/ops/utils/op_kernel/operator/catlass/act/gemv_coord.hpp
index 2e925c4a..7b5d6d90 100644
--- a/csrc/deepep/ops/utils/op_kernel/operator/catlass/act/gemv_coord.hpp
+++ b/csrc/deepep/ops/utils/op_kernel/operator/catlass/act/gemv_coord.hpp
@@ -27,9 +27,9 @@ struct GemvShape {
     static constexpr uint32_t M = M_;
     static constexpr uint32_t N = N_;
 
-    static constexpr int64_t MN = M * N;
+    static constexpr uint64_t MN = static_cast<uint64_t>(M) * static_cast<uint64_t>(N);
 
-    static constexpr int64_t COUNT = MN;
+    static constexpr uint64_t COUNT = MN;
 
     /// Returns a Coord object
     ACT_HOST_DEVICE
