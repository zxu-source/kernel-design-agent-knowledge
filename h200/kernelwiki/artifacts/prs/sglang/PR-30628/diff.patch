diff --git a/python/sglang/srt/speculative/triton_ops/multi_layer_eagle.py b/python/sglang/srt/speculative/triton_ops/multi_layer_eagle.py
index a41c9f1d7dc7..7c657d103adb 100644
--- a/python/sglang/srt/speculative/triton_ops/multi_layer_eagle.py
+++ b/python/sglang/srt/speculative/triton_ops/multi_layer_eagle.py
@@ -15,8 +15,9 @@
 import triton
 import triton.language as tl
 
-from sglang.srt.utils import is_cpu
+from sglang.srt.utils import is_cpu, is_npu
 
+_is_npu = is_npu()
 _is_cpu = is_cpu()
 
 if _is_cpu:
@@ -74,6 +75,12 @@ def rotate_input_ids(
         return input_ids
 
     batch_size = extend_seq_lens.shape[0]
+
+    # rotate_input_ids_triton skipped: batch_size=0 (empty extend_seq_lens).
+    # This is expected when a DP rank has no requests.
+    if batch_size == 0 and _is_npu:
+        return input_ids
+
     BLOCK_SIZE = 4096 if select_index is not None else 8
     grid = (batch_size,)
 
