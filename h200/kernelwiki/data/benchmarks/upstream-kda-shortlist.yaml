version: 1
scope: formal KDA comparison queue; promotion requires an exact semantic and timing contract
entries:
  - upstream_entry_id: upstream-gitee-c906238f3d07a69b
    category: gemm
    formal_baseline: torch.addmm or an equivalent BF16-input/FP32-output alpha-beta contract
    queue_status: needs-measurement-wrapper
    blocker: upstream source measures one launch with no warmup or repetitions
  - upstream_entry_id: upstream-gitee-32f48f6259b58e1e
    category: gemm
    formal_baseline: torch.addmm or an equivalent FP16-input/FP32-output alpha-beta contract
    queue_status: needs-measurement-wrapper
    blocker: upstream source measures one launch with no warmup or repetitions
  - upstream_entry_id: upstream-gitee-41dc70f17ae62c42
    category: gemm
    formal_baseline: torch.addmm or an equivalent TF32 alpha-beta contract with torch.backends.cuda.matmul.allow_tf32 enabled
    queue_status: needs-measurement-wrapper
    blocker: upstream source measures one launch with no warmup or repetitions
  - upstream_entry_id: upstream-gitee-1186a716aae34dde
    category: memory-pipeline
    formal_baseline: torch.matmul reference with identical FP32 matrix contract
    queue_status: validated-negative
    result: upstream 0.551 ms versus torch 0.1217440033 ms at 1280x1280 FP32; do not select for speedup
  - upstream_entry_id: upstream-gitee-ba300b6b5f4dbc17
    category: transpose
    formal_baseline: torch.transpose(...).contiguous()
    queue_status: validated-positive-microbenchmark
    result: upstream optimized 0.00326 ms versus torch 0.00974496 ms at 1024x1024 FP32; not a KDA replay
promotion_gate:
  - exact KDA layout/dtype/semantic contract
  - small medium large representative shapes
  - correctness versus formal_baseline
  - raw H200 latency output
  - same warmup and timed-iteration policy
